// Build-time constant injected by Vite `define` (see vite.config.ts /
// vitest.config.ts). Holds the short git commit of the built image, or 'dev'
// for local builds and tests.
declare const __GIT_COMMIT__: string;
